<SlSelect label="{{label}}" name={`{{field_name~}}{{#if vec}}[${i}]{{/if}}`} onSlChange={ (e: any) => handleChange({event: e, data: {type: e.target.value}}) }{{#if vec}} key={i}{{/if}} {{#if required}} required{{/if}} >{{#each field_type.variants}} <SlOption value="{{title_case this}}">{{title_case this}}</SlOption> {{/each}} </SlSelect>