<SlSelect label="{{label}}" name="{{field_name}}" onSlChange={ (e: any) => handleChange({type: e.target.value}) }{{#if required}} required{{/if}} >{{#each field_type.variants}} <SlOption value="{{kebab_case this}}">{{title_case this}}</SlOption> {{/each}} </SlSelect>