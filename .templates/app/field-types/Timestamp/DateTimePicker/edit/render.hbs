<VdDateTime label="{{label}}" name="{{field_name}}" value={ ({{~entry_name}}.{{field_name~}}).toISOString() } onChange={ (e: any) => { e.target.data = +(new Date(e.target.data)); handleChange({event: e}) } }{{#if required}} required{{/if}} />
