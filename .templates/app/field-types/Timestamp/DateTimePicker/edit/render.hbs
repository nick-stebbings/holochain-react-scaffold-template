<VdDateTime label="{{label}}" name="{{field_name}}" value={ new Date({{~entry_name}}.{{field_name~}}).toISOString() } onChange={ (e: any) => { e.target.value = +(new Date(e.target.value)); handleChange({event: e}) } }{{#if required}} required{{/if}} />
