{{#if (eq collection_type.type "Global")}}
At first, the UI for this application is empty. If you want the newly scaffolded collection to be the entry point for its UI, import the element in `ui/src/App.tsx`:

  import {{pascal_case collection_name}} from './{{dna_role_name}}/{{coordinator_zome_manifest.name}}/features/{{snake_case referenceable.name}}/{{pascal_case collection_name}}.tsx';

Add it into the render method/returned JSX for the `App` component: 
  <{{pascal_case collection_name}}></{{pascal_case collection_name}}>
{{/if}}
